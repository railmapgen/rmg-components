import { ComponentStyleConfig } from '@chakra-ui/theme';

export const rmgSectionTheme: ComponentStyleConfig = {
    baseStyle: {
        position: 'relative',
        background: 'inherit',
    },
};
