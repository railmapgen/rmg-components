import { ComponentStyleConfig } from '@chakra-ui/theme';

export const rmgWindowTheme: ComponentStyleConfig = {
    baseStyle: {
        flexDirection: 'column',
        height: '100%',
        overflow: 'hidden',
    },
};
