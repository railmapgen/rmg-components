import { ComponentStyleConfig } from '@chakra-ui/theme';

export const rmgWindowHeaderTheme: ComponentStyleConfig = {
    baseStyle: {
        px: 2,
        alignItems: 'center',
        zIndex: 15,
    },
};
